{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Open Weather Wardrobe",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}